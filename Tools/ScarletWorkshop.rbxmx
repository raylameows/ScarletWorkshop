<roblox xmlns:xmime="http://www.w3.org/2005/05/xmlmime" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="http://www.roblox.com/roblox.xsd" version="4">
	<External>null</External>
	<External>nil</External>
	<Item class="Folder" referent="RBX2E7BEDB5F698496A9F9DC451ABD768DA">
		<Properties>
			<BinaryString name="AttributesSerialize"></BinaryString>
			<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
			<bool name="DefinesCapabilities">false</bool>
			<string name="Name">ScarletWorkshop</string>
			<int64 name="SourceAssetId">-1</int64>
			<BinaryString name="Tags"></BinaryString>
		</Properties>
		<Item class="Folder" referent="RBX9DCA16D1F2944C2E8668E0CE25275803">
			<Properties>
				<BinaryString name="AttributesSerialize"></BinaryString>
				<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
				<bool name="DefinesCapabilities">false</bool>
				<string name="Name">LightingPort</string>
				<int64 name="SourceAssetId">-1</int64>
				<BinaryString name="Tags"></BinaryString>
			</Properties>
			<Item class="ModuleScript" referent="RBXD7B61821A1714DA8843D29F42B29BC85">
				<Properties>
					<Content name="LinkedSource"><null></null></Content>
					<ProtectedString name="Source"><![CDATA[local CoreGui = game:GetService("CoreGui")
local Lighting = game:GetService("Lighting")

local Create = require(`../Util/UI/Create`)
local Frame = require(`../Util/UI/Frame`)
local Label = require(`../Util/UI/Label`)
local Button = require(`../Util/UI/Button`)
local Seperator = require(`../Util/UI/Seperator`)

local Current = require(`../Util/Current`)
local Instances = require(`../Serializer/Instances`)
local TypeToValueBase = require(`../Util/TypeToValueBase`)

local LightingPort = {}
LightingPort.Name = `Lighting Port` :: string
LightingPort.Description = `Allows you to easily preview props in studio` :: string
LightingPort.Icon = 96681177725941 :: number

local function IsLightingEffect(object: Instance)
	return object:IsA(`PostEffect`) or object:IsA(`Atmosphere`) or object:IsA(`Sky`)
end

function LightingPort:Export()
	local current = Current:Get()
	if not current then return end
	
	local previous = current:FindFirstChild(`Lighting`)
	if previous then previous:Destroy() end
	
	local lightingFolder = Instance.new(`Folder`)
	lightingFolder.Name = `Lighting`

	local properties = Instance.new(`Folder`)
	properties.Name = `Properties`
	properties.Parent = lightingFolder

	local effects = Instance.new(`Folder`)
	effects.Name = `Effects`
	effects.Parent = lightingFolder
	
	for _, propertyData in (Instances.Lighting) do
		local property, propertyType, default = unpack(propertyData)
		local class = TypeToValueBase(propertyType)
		if not class then
			warn(`Can't convert {property}'s type {propertyType} to value base`)
			continue
		end
		
		local object: ValueBase = Instance.new(class)
		object.Value = Lighting[property]
		object.Name = property
		object.Parent = properties
	end
	
	for _, effect in (Lighting:GetChildren()) do
		if not IsLightingEffect(effect) then continue end 
		
		local copy = effect:Clone()
		copy.Parent = effects
	end
	
	lightingFolder.Parent = current
end

function LightingPort:Import()
	local current = Current:Get()
	if not current then return end
	
	local lightingFolder = current:FindFirstChild(`Lighting`)
	if not lightingFolder then return end
	
	for _, effect in (Lighting:GetChildren()) do
		if not IsLightingEffect(effect) then continue end
		effect:Destroy()
	end
	
	for _, lightingPropertyObject in (lightingFolder.Properties:GetChildren()) do
		local property, value = lightingPropertyObject.Name, lightingPropertyObject.Value
		Lighting[property] = value
	end
	
	if lightingFolder:FindFirstChild(`Effects`) then
		for _, effect in (lightingFolder.Effects:GetChildren()) do
			local copy = effect:Clone()
			copy.Parent = Lighting
		end
	else
		for _, effect in (lightingFolder:GetChildren()) do
			if not IsLightingEffect(effect) then continue end
			
			local copy = effect:Clone()
			copy.Parent = Lighting
		end
	end
end

function LightingPort:Clear()
	for _, effect in (Lighting:GetChildren()) do
		if not IsLightingEffect(effect) then continue end
		effect:Destroy()
	end
	
	local defaultsFolder = Lighting:FindFirstChild(`EditingLighting`)
	for _, propertyData in (Instances.Lighting) do
		local property, propertyType, default = unpack(propertyData)
		local set = defaultsFolder and defaultsFolder.Properties:FindFirstChild(property) and defaultsFolder.Properties[property].Value or default
		Lighting[property] = set
	end
	
	if defaultsFolder then
		for _, effect in (defaultsFolder.Effects:GetChildren()) do
			local copy = effect:Clone()
			copy.Parent = Lighting
		end
	end
end

function LightingPort:Activate()
	self.UI = Create "ScreenGui" {
		IgnoreGuiInset = true,

		Parent = CoreGui,
		Children = {
			Button {
				Text = `Export`,
				Size = UDim2.fromOffset(200, 30),
				Position = UDim2.fromOffset(50, 50),

				Activated = function()
					self:Export()
				end,
			},
			Button {
				Text = `Import`,
				Size = UDim2.fromOffset(200, 30),
				Position = UDim2.fromOffset(270, 50),

				Activated = function()
					self:Import()
				end,
			},
			Button {
				Text = `Clear`,
				Size = UDim2.fromOffset(200, 30),
				Position = UDim2.fromOffset(490, 50),

				Activated = function()
					self:Clear()
				end,
			},
		}
	}
end

function LightingPort:Deactivate()
	if self.UI then
		self.UI:Destroy()
		self.UI = nil
	end
end

return LightingPort]]></ProtectedString>
					<string name="ScriptGuid">{6D3E6740-7537-433C-B716-6C4E86375D97}</string>
					<BinaryString name="AttributesSerialize"></BinaryString>
					<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
					<bool name="DefinesCapabilities">false</bool>
					<string name="Name">Init</string>
					<int64 name="SourceAssetId">-1</int64>
					<BinaryString name="Tags"></BinaryString>
				</Properties>
			</Item>
		</Item>
		<Item class="Script" referent="RBXBAC6B0CEA9064AD3B8A6BB6A137DCC6F">
			<Properties>
				<ProtectedString name="Source"><![CDATA[local PluginDebugService = game:GetService("PluginDebugService")
local RunService = game:GetService("RunService")
local Selection = game:GetService("Selection")
local UserInputService = game:GetService("UserInputService")
if not RunService:IsStudio() then return end

local FeatureCheck = require(`./Util/FeatureCheck`)

local Toolbar: PluginToolbar = plugin:CreateToolbar(`ScarletUtilityTools`)
local Plugins: {Folder} = {} do
	for _,folder in (script.Parent:GetChildren()) do
		if folder:IsA(`Folder`) and folder:FindFirstChild(`Init`) then
			table.insert(Plugins, folder)
		end
	end
end

local Current: {}? = nil
for _, tool in (Plugins) do
	local module = require(tool.Init)
	if not module.Name then continue end
	module.Plugin = plugin
	
	local button = Toolbar:CreateButton(module.Name, module.Description or module.Name, `rbxassetid://{module.Icon}`)
	button.Click:Connect(function()
		if Current then
			local currentIsSame = Current == module
			Deactivation()
			
			if currentIsSame then 
				plugin:Deactivate()
				return
			end
		end
		
		plugin:Activate(true)
		module:Activate()
		Current = module
	end)
end

-- Searching for the plugin inside PluginDebugService is very painful especially because of the icons and the user_ prefix
local quickSelect: RBXScriptConnection?
if FeatureCheck(`Debug`) then
	quickSelect = UserInputService.InputBegan:Connect(function(input: InputObject, gpe: boolean)
		if gpe then return end
		
		if input.KeyCode == Enum.KeyCode.F then
			local object = PluginDebugService:FindFirstChild(`user_ScarletUtilityTools.rbxmx`)
			Selection:Set({object})
		end
	end)
end

function Deactivation()
	if Current then
		Current:Deactivate()
		Current = nil
	end
end

function Unloading()
	if quickSelect then
		quickSelect:Disconnect()
		quickSelect = nil
	end
	Deactivation()
end

plugin.Deactivation:Connect(Deactivation)
plugin.Unloading:Connect(Unloading)]]></ProtectedString>
				<bool name="Disabled">false</bool>
				<Content name="LinkedSource"><null></null></Content>
				<token name="RunContext">3</token>
				<string name="ScriptGuid">{B99809BC-EB3B-4397-974C-EAA40A29A68A}</string>
				<BinaryString name="AttributesSerialize"></BinaryString>
				<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
				<bool name="DefinesCapabilities">false</bool>
				<string name="Name">Main</string>
				<int64 name="SourceAssetId">-1</int64>
				<BinaryString name="Tags"></BinaryString>
			</Properties>
		</Item>
		<Item class="Folder" referent="RBXD8D528BF90C740F89E79C7D9F9178247">
			<Properties>
				<BinaryString name="AttributesSerialize"></BinaryString>
				<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
				<bool name="DefinesCapabilities">false</bool>
				<string name="Name">Util</string>
				<int64 name="SourceAssetId">-1</int64>
				<BinaryString name="Tags"></BinaryString>
			</Properties>
			<Item class="Folder" referent="RBXB7931DE893204AA9B31B7E1F108480EC">
				<Properties>
					<BinaryString name="AttributesSerialize"></BinaryString>
					<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
					<bool name="DefinesCapabilities">false</bool>
					<string name="Name">UI</string>
					<int64 name="SourceAssetId">-1</int64>
					<BinaryString name="Tags"></BinaryString>
				</Properties>
				<Item class="ModuleScript" referent="RBXDBC561C6114E4A3BBECEC3A56F59C15E">
					<Properties>
						<Content name="LinkedSource"><null></null></Content>
						<ProtectedString name="Source"><![CDATA[export type Class = string
export type Properties = {[string]: any}
export type Call = (class: Class) -> ((properties: Properties) -> Instance)
export type Wrapped<Defaults> = (properties: {[Defaults]: any}) -> Instance

local Create
Create = function(class: Class)
	return function(properties: Properties)
		local object: Instance = Instance.new(class)
		local parent: Instance?
		
		for k, v in (properties) do
			if k == `Parent` then
				parent = v
			elseif k == `Children` then
				for _, child in (v) do
					if typeof(child) == `table` then
						Create (child[1]) (child[2]).Parent = object
					else
						child.Parent = object
					end
				end
			else
				if typeof(v) == `function` then
					object[k]:Connect(v)
				else
					object[k] = v
				end
			end
		end
		
		if parent then
			object.Parent = parent
		end
		
		return object
	end
end

return Create]]></ProtectedString>
						<string name="ScriptGuid">{A9CA080E-8D05-496E-BAEF-681376EF9404}</string>
						<BinaryString name="AttributesSerialize"></BinaryString>
						<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
						<bool name="DefinesCapabilities">false</bool>
						<string name="Name">Create</string>
						<int64 name="SourceAssetId">-1</int64>
						<BinaryString name="Tags"></BinaryString>
					</Properties>
				</Item>
				<Item class="ModuleScript" referent="RBXBBB53643E0454263A04D9E7FD3F9A812">
					<Properties>
						<Content name="LinkedSource"><null></null></Content>
						<ProtectedString name="Source"><![CDATA[local Create = require(`./Create`)
local Wrapper = require(`./Wrapper`)
local Pseudo = require(`./Pseudo`)

local Defaults = {
	BackgroundColor3 = Color3.fromRGB(0, 0, 0),
	BackgroundTransparency = 0.5,
	BorderSizePixel = 0,
	
	Children = {
		UICorner = Pseudo "UICorner" {
			CornerRadius = UDim.new(0, 7)
		}
	}
}

return Wrapper "Frame" (Defaults) :: Create.Wrapped<keyof<typeof(Defaults)>>]]></ProtectedString>
						<string name="ScriptGuid">{E40CCC08-5D74-475E-B90A-B4D56D13CE7E}</string>
						<BinaryString name="AttributesSerialize"></BinaryString>
						<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
						<bool name="DefinesCapabilities">false</bool>
						<string name="Name">Frame</string>
						<int64 name="SourceAssetId">-1</int64>
						<BinaryString name="Tags"></BinaryString>
					</Properties>
				</Item>
				<Item class="ModuleScript" referent="RBX5DD2AAFCADEB442A99287CC4A9B14F9E">
					<Properties>
						<Content name="LinkedSource"><null></null></Content>
						<ProtectedString name="Source"><![CDATA[local Create = require(`./Create`)

return function(class: Create.Class)
	return function(defaults: Create.Properties)
		return function(properties: Create.Properties)
			for k, v in (defaults) do
				if properties[k] then 
					if k == `Children` then
						for _, child in (v) do
							table.insert(properties.Children, child)
						end
					end
					
					continue 
				end
				properties[k] = v
			end
			
			return Create (class) (properties)
		end
	end
end]]></ProtectedString>
						<string name="ScriptGuid">{12171715-FAB4-4196-BCD6-2C0B46B36CFE}</string>
						<BinaryString name="AttributesSerialize"></BinaryString>
						<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
						<bool name="DefinesCapabilities">false</bool>
						<string name="Name">Wrapper</string>
						<int64 name="SourceAssetId">-1</int64>
						<BinaryString name="Tags"></BinaryString>
					</Properties>
				</Item>
				<Item class="ModuleScript" referent="RBX23D1C590025543CE9F4DD85471F845BD">
					<Properties>
						<Content name="LinkedSource"><null></null></Content>
						<ProtectedString name="Source"><![CDATA[local Create = require(`./Create`)
local Wrapper = require(`./Wrapper`)

return Wrapper "TextLabel" {
	Font = Enum.Font.FredokaOne,
	BackgroundTransparency = 1,
	TextScaled = true,
	TextColor3 = Color3.fromRGB(255, 255, 255),
}]]></ProtectedString>
						<string name="ScriptGuid">{01937A36-D237-4212-8205-23735CF65A57}</string>
						<BinaryString name="AttributesSerialize"></BinaryString>
						<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
						<bool name="DefinesCapabilities">false</bool>
						<string name="Name">Label</string>
						<int64 name="SourceAssetId">-1</int64>
						<BinaryString name="Tags"></BinaryString>
					</Properties>
				</Item>
				<Item class="ModuleScript" referent="RBX3418CF6A4C4A4511BCF8CF5D7EEFBC75">
					<Properties>
						<Content name="LinkedSource"><null></null></Content>
						<ProtectedString name="Source"><![CDATA[local Create = require(`./Create`)
local Wrapper = require(`./Wrapper`)
local Pseudo = require(`./Pseudo`)

local Defaults = {
	Font = Enum.Font.FredokaOne,
	BackgroundTransparency = 0.5,
	TextScaled = true,
	TextColor3 = Color3.fromRGB(255, 255, 255),
	BackgroundColor3 = Color3.fromRGB(0, 0, 0),

	Children = {
		UICorner = Pseudo "UICorner" {
			CornerRadius = UDim.new(0, 7)
		}
	}
}

return Wrapper "TextButton" (Defaults) :: Create.Wrapped<keyof<typeof(Defaults)>>]]></ProtectedString>
						<string name="ScriptGuid">{F0F4AB3F-2CCE-472B-94EB-52EAAF1D3960}</string>
						<BinaryString name="AttributesSerialize"></BinaryString>
						<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
						<bool name="DefinesCapabilities">false</bool>
						<string name="Name">Button</string>
						<int64 name="SourceAssetId">-1</int64>
						<BinaryString name="Tags"></BinaryString>
					</Properties>
				</Item>
				<Item class="ModuleScript" referent="RBXDDA9A86BBB6C48F6821C4FA22326591C">
					<Properties>
						<Content name="LinkedSource"><null></null></Content>
						<ProtectedString name="Source"><![CDATA[local Create = require(`./Create`)

return function(class: Create.Class)
	return function(properties: Create.Properties)
		return {class, properties}
	end
end]]></ProtectedString>
						<string name="ScriptGuid">{85ACDF25-F58A-41FA-A794-8BDFDD86D626}</string>
						<BinaryString name="AttributesSerialize"></BinaryString>
						<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
						<bool name="DefinesCapabilities">false</bool>
						<string name="Name">Pseudo</string>
						<int64 name="SourceAssetId">-1</int64>
						<BinaryString name="Tags"></BinaryString>
					</Properties>
				</Item>
				<Item class="ModuleScript" referent="RBX587223EC70074CAFAF4527CD3FBB3758">
					<Properties>
						<Content name="LinkedSource"><null></null></Content>
						<ProtectedString name="Source"><![CDATA[local Create = require(`./Create`)
local Wrapper = require(`./Wrapper`)
local Pseudo = require(`./Pseudo`)

local Defaults = {
	PlaceholderText = `Placeholder`,
	Text = ``,
	
	Font = Enum.Font.FredokaOne,
	BackgroundTransparency = 0.5,
	TextScaled = true,
	TextColor3 = Color3.fromRGB(255, 255, 255),
	BackgroundColor3 = Color3.fromRGB(0, 0, 0),
	PlaceholderColor3 = Color3.fromRGB(150, 150, 150),

	Children = {
		UICorner = Pseudo "UICorner" {
			CornerRadius = UDim.new(0, 7)
		}
	}
}

return Wrapper "TextBox" (Defaults) :: Create.Wrapped<keyof<typeof(Defaults)>>]]></ProtectedString>
						<string name="ScriptGuid">{9F1EE691-3B70-4041-830D-B01D582132F4}</string>
						<BinaryString name="AttributesSerialize"></BinaryString>
						<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
						<bool name="DefinesCapabilities">false</bool>
						<string name="Name">TextBox</string>
						<int64 name="SourceAssetId">-1</int64>
						<BinaryString name="Tags"></BinaryString>
					</Properties>
				</Item>
				<Item class="ModuleScript" referent="RBX9D815A8D4792496BA278A825DF26967B">
					<Properties>
						<Content name="LinkedSource"><null></null></Content>
						<ProtectedString name="Source"><![CDATA[local Create = require(`./Create`)
local Wrapper = require(`./Wrapper`)
local Pseudo = require(`./Pseudo`)

local Defaults = {
	BackgroundColor3 = Color3.fromRGB(255, 255, 255),
	BackgroundTransparency = 0,
	Size = UDim2.new(1, 0, 0, 2),
	BorderSizePixel = 0
}

return Wrapper "Frame" (Defaults) :: Create.Wrapped<keyof<typeof(Defaults)>>]]></ProtectedString>
						<string name="ScriptGuid">{C3DE2340-0EA8-4B11-8B5A-3B8EF36BBD9C}</string>
						<BinaryString name="AttributesSerialize"></BinaryString>
						<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
						<bool name="DefinesCapabilities">false</bool>
						<string name="Name">Seperator</string>
						<int64 name="SourceAssetId">-1</int64>
						<BinaryString name="Tags"></BinaryString>
					</Properties>
				</Item>
			</Item>
			<Item class="ModuleScript" referent="RBX952EE6C0F7EC46AB99DEE20FCCC0185C">
				<Properties>
					<Content name="LinkedSource"><null></null></Content>
					<ProtectedString name="Source"><![CDATA[return function(feature: string)
	return workspace:GetAttribute(feature)
end]]></ProtectedString>
					<string name="ScriptGuid">{5E311A9D-4651-4644-BE38-60D243696E0B}</string>
					<BinaryString name="AttributesSerialize"></BinaryString>
					<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
					<bool name="DefinesCapabilities">false</bool>
					<string name="Name">FeatureCheck</string>
					<int64 name="SourceAssetId">-1</int64>
					<BinaryString name="Tags"></BinaryString>
				</Properties>
			</Item>
			<Item class="ModuleScript" referent="RBXC76DF759F26A4679B9E847B71E65F7D0">
				<Properties>
					<Content name="LinkedSource"><null></null></Content>
					<ProtectedString name="Source"><![CDATA[return function(path: string): Instance
	local iterate = string.split(path, `.`)
	local current = game
	for index, traverse in (iterate) do
		local found = current:FindFirstChild(traverse)
		if not found then
			found = Instance.new(`Folder`)
			found.Name = traverse
			found.Parent = current
		end
		
		current = found
	end
	
	return current
end]]></ProtectedString>
					<string name="ScriptGuid">{35009302-1C53-4401-B783-4D6BE54658F8}</string>
					<BinaryString name="AttributesSerialize"></BinaryString>
					<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
					<bool name="DefinesCapabilities">false</bool>
					<string name="Name">Traverse</string>
					<int64 name="SourceAssetId">-1</int64>
					<BinaryString name="Tags"></BinaryString>
				</Properties>
			</Item>
			<Item class="ModuleScript" referent="RBX95F6339E8F1F411BA0AAD5C0D6BAC6DF">
				<Properties>
					<Content name="LinkedSource"><null></null></Content>
					<ProtectedString name="Source"><![CDATA[local Visualize

local PriorityOrder = {
	Class = 1,
	Config = 2,
	Properties = 3,
	Attributes = 4,
	Children = 6,
}
local ValueHandlers = {
	[`table`] = function(value, indent, indentStr, nextIndentStr)
		if next(value) == nil then
			return "{}"
		end

		local result = "{\n"
		local keys = {}
		for k in (value) do
			table.insert(keys, k)
		end

		table.sort(keys, function(a, b)
			local aPriority = PriorityOrder[a]
			local bPriority = PriorityOrder[b]
			if aPriority and bPriority then
				return aPriority < bPriority
			end
			if aPriority then return true end
			if bPriority then return false end
			return tostring(a) < tostring(b)
		end)

		for _, k in ipairs(keys) do
			local v = value[k]

			local key
			if typeof(k) == "string" and k:match("^%a[%w_]*$") then
				key = k
			else
				key = "[" .. Visualize(k, indent + 1) .. "]"
			end

			result ..= nextIndentStr .. key .. " = " .. Visualize(v, indent + 1) .. ",\n"
		end

		result ..= indentStr .. "}"
		return result
	end,
	[`string`] = function(value)
		if value:find("\n") then
			return "[[\n" .. value .. "]]"
		elseif value:find(`"`) then
			return "[[" .. value .. "]]"
		else
			return string.format("%q", value)
		end
	end,
	[`number`] = tostring,
	[`boolean`] = tostring,
	[`Vector3`] = function(value)
		return `Vector3.new({value.X}, {value.Y}, {value.Z})`
	end,
	[`Color3`] = function(value)
		return `Color3.new({value.R}, {value.G}, {value.B})`
	end,
	[`CFrame`] = function(value)
		return `CFrame.new({tostring(value)})`
	end,
	[`EnumItem`] = function(value)
		return tostring(value)
	end,
	[`nil`] = function()
		return "nil"
	end,
	[`unknown`] = function(value, _, _, _, valueType)
		return `nil --[[({valueType}) | ({tostring(value)})]]`
	end,
}

Visualize = function(value, indent)
	indent = indent or 0

	local indentStr = string.rep("\t", indent)
	local nextIndentStr = string.rep("\t", indent + 1)

	local valueType = typeof(value)
	local handler = ValueHandlers[valueType]
	local pass = {value, indent, indentStr, nextIndentStr, valueType}

	if handler then
		return handler(unpack(pass))
	else
		return ValueHandlers.unknown(unpack(pass))
	end
end

return Visualize]]></ProtectedString>
					<string name="ScriptGuid">{1910EE71-C396-40A3-91D5-E5549B5D1158}</string>
					<BinaryString name="AttributesSerialize"></BinaryString>
					<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
					<bool name="DefinesCapabilities">false</bool>
					<string name="Name">NodeTreeVisualizer</string>
					<int64 name="SourceAssetId">-1</int64>
					<BinaryString name="Tags"></BinaryString>
				</Properties>
			</Item>
			<Item class="ModuleScript" referent="RBX317272FF83B441A6AF8389DBC52AEC4E">
				<Properties>
					<Content name="LinkedSource"><null></null></Content>
					<ProtectedString name="Source"><![CDATA[local ReplicatedStorage = game:GetService("ReplicatedStorage")

local Traverse = require(`./Traverse`)
local Maps = Traverse(`ServerStorage.Maps`)

local Current = {}

function Current:Fetch(): ObjectValue
	local current: ObjectValue? = Maps:FindFirstChild(`Current`)
	if not current then
		current = Instance.new(`ObjectValue`)
		current.Name = `Current`
		current.Value = nil
		current.Parent = Maps
	end

	return current
end

function Current:Set(set: Instance?): Instance?
	local current = self:Fetch()
	current.Value = set
	return set
end

function Current:Get(): Instance?
	local current = self:Fetch()
	return current.Value
end

return Current]]></ProtectedString>
					<string name="ScriptGuid">{443F4094-A13A-4E4E-BC83-9A32E7A3E9A6}</string>
					<BinaryString name="AttributesSerialize"></BinaryString>
					<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
					<bool name="DefinesCapabilities">false</bool>
					<string name="Name">Current</string>
					<int64 name="SourceAssetId">-1</int64>
					<BinaryString name="Tags"></BinaryString>
				</Properties>
			</Item>
			<Item class="ModuleScript" referent="RBXFBF5A9FCBA70425BA71F9FFBA6342BBD">
				<Properties>
					<Content name="LinkedSource"><null></null></Content>
					<ProtectedString name="Source"><![CDATA[local function Singleton<T>(value: T | ""): T
	return value
end

local TypeToValueBase = {
	boolean = Singleton "BoolValue",
	string = Singleton "StringValue",
	number = Singleton "NumberValue",
	Instance = Singleton "ObjectValue",
	BrickColor = Singleton "BrickColorValue",
	CFrame = Singleton "CFrameValue",
	Color3 = Singleton "Color3Value",
	Vector3 = Singleton "Vector3Value",
}

type InputType = keyof<typeof(TypeToValueBase)>
type ValueBase = index<typeof(TypeToValueBase), InputType>

return function(inputType: InputType): ValueBase
	local class = TypeToValueBase[inputType]
	return class
end]]></ProtectedString>
					<string name="ScriptGuid">{4C87DBF6-83EA-49C0-8FC9-9140F077F511}</string>
					<BinaryString name="AttributesSerialize"></BinaryString>
					<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
					<bool name="DefinesCapabilities">false</bool>
					<string name="Name">TypeToValueBase</string>
					<int64 name="SourceAssetId">-1</int64>
					<BinaryString name="Tags"></BinaryString>
				</Properties>
			</Item>
			<Item class="ModuleScript" referent="RBXD27A16C6037C4A9DA42D8CA7A9D1A66F">
				<Properties>
					<Content name="LinkedSource"><null></null></Content>
					<ProtectedString name="Source"><![CDATA[return function<T>(func: () -> T): (T?, any)
	local success, result = pcall(func)

	if success then
		return result, nil
	else
		return nil, result
	end
end]]></ProtectedString>
					<string name="ScriptGuid">{C54F65C6-3397-453D-B12B-22DE1D012548}</string>
					<BinaryString name="AttributesSerialize"></BinaryString>
					<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
					<bool name="DefinesCapabilities">false</bool>
					<string name="Name">SafeCall</string>
					<int64 name="SourceAssetId">-1</int64>
					<BinaryString name="Tags"></BinaryString>
				</Properties>
			</Item>
		</Item>
		<Item class="Folder" referent="RBXC432AD5F12C749C5BCFE37361CBD8C32">
			<Properties>
				<BinaryString name="AttributesSerialize"></BinaryString>
				<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
				<bool name="DefinesCapabilities">false</bool>
				<string name="Name">MapSelector</string>
				<int64 name="SourceAssetId">-1</int64>
				<BinaryString name="Tags"></BinaryString>
			</Properties>
			<Item class="ModuleScript" referent="RBX891C3E9D7F3B48F7AF263AB3F656EE6D">
				<Properties>
					<Content name="LinkedSource"><null></null></Content>
					<ProtectedString name="Source"><![CDATA[local CoreGui = game:GetService("CoreGui")
local Selection = game:GetService("Selection")

local Create = require(`../Util/UI/Create`)
local Frame = require(`../Util/UI/Frame`)
local Label = require(`../Util/UI/Label`)
local Button = require(`../Util/UI/Button`)
local Seperator = require(`../Util/UI/Seperator`)

local Current = require(`../Util/Current`)
local Traverse = require(`../Util/Traverse`)

local MapSelector = {}
MapSelector.Name = `Map Selector` :: string
MapSelector.Description = `Searches through all available maps in the place and allows you to select all editables` :: string
MapSelector.Icon = 117093940689396 :: number

local Maps = Traverse(`ServerStorage.Maps`)
if Maps:FindFirstChild(`PvP`) then Maps = Maps.PvP :: Instance end

function MapSelector:Show()
	if not self.Selected then return end
	self.Selected.Parent = workspace
end

function MapSelector:Hide()
	if not self.Selected then return end
	self.Selected.Parent = Maps
end

function MapSelector:Select(newMap: Folder?)
	self:Hide()
	self.Selected = Current:Set(newMap)
	self:Refresh()
	self:Show()
end

function MapSelector:Refresh()
	for index, button in (self.MapList:GetChildren()) do
		if not button:IsA(`TextButton`) then continue end
		
		if button.Map.Value == self.Selected then
			button.BackgroundColor3 = Color3.new(1, 1, 1)
		else
			button.BackgroundColor3 = Color3.new(0, 0, 0)
		end
	end
end

function MapSelector:Add(map: Instance)
	if not map or not map:IsA(`Folder`) then return end

	Button {
		Text = map.Name,
		Size = UDim2.new(1, 0, 0, 30),
		Parent = self.MapList,
		BackgroundTransparency = 0.75,

		Children = {
			Create "UITextSizeConstraint" {
				MaxTextSize = 17
			},
			Create "ObjectValue" {
				Name = `Map`,
				Value = map
			}
		},

		Activated = function()
			if map == self.Selected then
				self:Select(nil)
			else
				self:Select(map)
			end
		end
	}
end

function MapSelector:ToggleAddButton(visible: boolean)
	if self.AddButton.Visible ~= visible then
		self.AddButton.Visible = visible
	end
end

function MapSelector:UpdateAddButton()
	local selected = Selection:Get()
	if #selected > 1 or #selected == 0 or (selected[1] and (self.Selected == selected[1] or selected[1].Parent == Maps) or not selected[1]:IsA(`Folder`)) then 
		self:ToggleAddButton(false)
	else
		self:ToggleAddButton(true)
	end
end

function MapSelector:List()
	for _, button in (self.MapList:QueryDescendants(`>TextButton`)) do
		button:Destroy()
	end
	
	local maps = Maps:GetChildren()
	if #maps == 0 and not self.Selected then
		for _, possibleMap in (workspace:QueryDescendants(`>Folder`)) do
			self:Add(possibleMap)
		end
	else
		for _, map in (maps) do
			self:Add(map)
		end
		self:Add(self.Selected)
	end
	self:Refresh()
end

function MapSelector:Activate()
	self.Selected = Current:Get()
	self.MapList = Create "ScrollingFrame" {
		Size = UDim2.new(1, 0, 1, -32),
		Position = UDim2.new(0, 0, 0, 32),
		BackgroundTransparency = 1,
		CanvasSize = UDim2.new(0, 0, 0, 0),
		AutomaticCanvasSize = Enum.AutomaticSize.Y,
		BottomImage = "rbxassetid://132155327",
		TopImage = "rbxassetid://132155327",
		BorderSizePixel = 0,
		ScrollBarThickness = 5,
		Children = {
			Create "UIListLayout" {
				Padding = UDim.new(0, 1)
			}
		}
	}
	self.AddButton = Button {
		Text = `Add`,

		Size = UDim2.fromOffset(200, 30),
		Position = UDim2.fromOffset(270, 50),

		Activated = function()
			local map = Selection:Get()[1]
			Selection:Set({})
			map.Parent = Maps
		end,
	}
	
	self.SelectionEvent = Selection.SelectionChanged:Connect(function()
		self:UpdateAddButton()
	end)
	self:UpdateAddButton()
	self:List()

	self.AddedEvent = Maps.ChildAdded:Connect(function()
		self:List()
	end)
	self.RemovedEvent = Maps.ChildRemoved:Connect(function()
		self:List()
	end)
	
	self.UI = Create "ScreenGui" {
		IgnoreGuiInset = true,

		Parent = CoreGui,
		Children = {
			Frame {
				Size = UDim2.new(0, 200, 1, -100),
				Position = UDim2.fromOffset(50, 50),

				Children = {
					Label {
						Text = `Maps`,
						Size = UDim2.new(1, 0, 0, 30)
					},
					Seperator {
						Position = UDim2.new(0, 0, 0, 30)
					},
					self.MapList,
				}
			},
			self.AddButton
		}
	}
end

function MapSelector:Deactivate()
	if self.UI then
		self.UI:Destroy()
		self.UI = nil
	end

	if self.SelectionEvent then
		self.SelectionEvent:Disconnect()
		self.SelectionEvent = nil
	end

	if self.AddedEvent then
		self.AddedEvent:Disconnect()
		self.AddedEvent = nil
	end

	if self.RemovedEvent then
		self.RemovedEvent:Disconnect()
		self.RemovedEvent = nil
	end
end

return MapSelector]]></ProtectedString>
					<string name="ScriptGuid">{F1E04401-6571-43F9-8002-229A47779FC1}</string>
					<BinaryString name="AttributesSerialize"></BinaryString>
					<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
					<bool name="DefinesCapabilities">false</bool>
					<string name="Name">Init</string>
					<int64 name="SourceAssetId">-1</int64>
					<BinaryString name="Tags"></BinaryString>
				</Properties>
			</Item>
		</Item>
		<Item class="Folder" referent="RBX23A1D35E82904BED9CB4C35EEDEB0A65">
			<Properties>
				<BinaryString name="AttributesSerialize"></BinaryString>
				<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
				<bool name="DefinesCapabilities">false</bool>
				<string name="Name">Serializer</string>
				<int64 name="SourceAssetId">-1</int64>
				<BinaryString name="Tags"></BinaryString>
			</Properties>
			<Item class="ModuleScript" referent="RBXADB3A413181043D88C78B83B48C2282C">
				<Properties>
					<Content name="LinkedSource"><null></null></Content>
					<ProtectedString name="Source"><![CDATA[local EncodingService = game:GetService("EncodingService")
local HttpService = game:GetService("HttpService")
local ScriptEditorService = game:GetService("ScriptEditorService")

local Shared = require(`./Shared`)
local Instances = require(`./Instances`)
local FeatureCheck = require(`../Util/FeatureCheck`)
local NodeTreeVisualizer = require(`../Util/NodeTreeVisualizer`)

local Write = {}
local RootNode: Shared.Node?
local RootObject: Instance?

local function WriteValue(value: any, valueType: string, propOrAtt: string?)
	local valueType = valueType or typeof(value)
	local call = Write[valueType]
	if not call then
		warn(`Type {valueType} of {propOrAtt and `{propOrAtt} ` or ``}{value} cannot be serialized`)
		return
	end

	return call(value)
end

local function GetIndex(object: Instance)
	local index = 1
	for _, child in (object.Parent:GetChildren()) do
		if child == object then
			return index
		elseif Instances[child.ClassName] ~= nil then
			index += 1
		end
	end

	return index
end

function Write.string(input: string)
	return input
end

function Write.number(number: number)
	return number
end

function Write.boolean(bool: boolean)
	return bool
end

function Write.Color3(color: Color3)
	return {color.R, color.G, color.B}
end

function Write.Vector3(vector: Vector3)
	return {vector.X, vector.Y, vector.Z}
end

function Write.CFrame(cf: CFrame)
	return {cf:GetComponents()}
end

function Write.UDim2(udim: UDim2)
	return {
		udim.X.Scale, udim.X.Offset,
		udim.Y.Scale, udim.Y.Offset
	}
end

function Write.EnumItem(enumItem: EnumItem)
	return tostring(enumItem)
end

function Write.BrickColor(color: BrickColor)
	return color.Number
end

function Write.NumberSequence(seq: NumberSequence)
	local keypoints = {}
	for _, k in (seq.Keypoints) do
		table.insert(keypoints, {
			Time = k.Time,
			Value = k.Value,
			Envelope = k.Envelope
		})
	end

	return keypoints
end

function Write.ColorSequence(seq: ColorSequence)
	local keypoints = {}
	for _, k in (seq.Keypoints) do
		table.insert(keypoints, {
			Time = k.Time,
			Color = Write.Color3(k.Value)
		})
	end

	return keypoints
end

function Write.NumberRange(range: NumberRange)
	return {range.Min, range.Max}
end

function Write.Rect(rect: Rect)
	return {
		rect.Min.X, rect.Min.Y,
		rect.Max.X, rect.Max.Y
	}
end

function Write.UDim(udim: UDim)
	return {udim.Scale, udim.Offset}
end

function Write.Vector2(vector: Vector2)
	return {vector.X, vector.Y}
end

function Write.InstanceReference(object: Instance?)
	if not object or not object:IsDescendantOf(RootObject) then return end
	
	local path = {}
	local current = object
	while current ~= RootObject do
		local index = GetIndex(current)
		if index then
			table.insert(path, index)
		end
		
		current = current.Parent
	end
	
	for i = 1, math.floor(#path / 2) do
		path[i], path[#path - i + 1] = path[#path - i + 1], path[i]
	end
	
	RootNode.Config.Expensives += 1
	return table.concat(path, `.`)
end

function Write.Instance(object: Instance, direct: boolean): Shared.Node
	if not Instances[object.ClassName] then
		warn(`Unknown instance {object.Name} of class {object.ClassName} will not be serialized`)
		return
	end
	
	local node: Shared.Node = {
		Class = object.ClassName,
		Properties = {},
		Attributes = {},
		Children = {}
	}
	if direct then
		RootNode = node
		RootNode.Config = {
			Expensives = 0,
		}
	end
	
	local availableProperties = Instances[object.ClassName]
	for _, propertyData in (availableProperties) do
		local property, propertyType, default = unpack(propertyData)
		if object[property] == default then continue end
		node.Properties[property] = WriteValue(object[property], propertyType, `property`)
	end
	
	for attribute, value in (object:GetAttributes()) do
		if attribute:sub(1, 3) == `RBX` then continue end
		node.Attributes[attribute] = WriteValue(value, nil, `attribute`)
	end
	
	for _, child in (object:GetChildren()) do
		local childNode = Write.Instance(child)
		if childNode == nil then continue end
		table.insert(node.Children, childNode)
	end
	
	return node
end

function Write.Map(map: Instance)
	RootObject = map:Clone()
	local nodeTree = Write.Instance(RootObject, true)
	if FeatureCheck(`NodeTree`) then
		local previous = workspace:FindFirstChild(`Node Tree`)
		if previous and previous:IsA(`ModuleScript`) then
			previous:Destroy()
		end
		
		local ms = Instance.new(`ModuleScript`)
		ScriptEditorService:UpdateSourceAsync(ms, function()
			return `return {NodeTreeVisualizer(nodeTree)}`
		end)
		ms.Name = `Node Tree`
		ms.Parent = workspace
		ScriptEditorService:OpenScriptDocumentAsync(ms)
	end
	
	nodeTree = HttpService:JSONEncode(RootNode)
	
	local nodeTreeBuffer = buffer.fromstring(nodeTree)
	local compressedNodeTree = EncodingService:Base64Encode(
		EncodingService:CompressBuffer(nodeTreeBuffer, Enum.CompressionAlgorithm.Zstd)
	)
	
	local compressedString = buffer.readstring(compressedNodeTree, 0, buffer.len(compressedNodeTree))
	if FeatureCheck(`Debug`) then
		print(`=== Compression Stats ===`)
		print(`Before Compression: {#nodeTree * 0.001}K`)
		print(`After Compression: {#compressedString * 0.001}K`)
	end
	
	RootNode = nil
	RootObject:Destroy()
	RootObject = nil
	return compressedString
end

return Write]]></ProtectedString>
					<string name="ScriptGuid">{3B987E87-D4FB-45A5-A7F0-EEFBD694250A}</string>
					<BinaryString name="AttributesSerialize"></BinaryString>
					<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
					<bool name="DefinesCapabilities">false</bool>
					<string name="Name">Write</string>
					<int64 name="SourceAssetId">-1</int64>
					<BinaryString name="Tags"></BinaryString>
				</Properties>
			</Item>
			<Item class="ModuleScript" referent="RBXD46114A52E8D404B8C470C3FBC921DC1">
				<Properties>
					<Content name="LinkedSource"><null></null></Content>
					<ProtectedString name="Source"><![CDATA[local AssetService = game:GetService("AssetService")
local EncodingService = game:GetService("EncodingService")
local HttpService = game:GetService("HttpService")

local Shared = require(`./Shared`)
local Instances = require(`./Instances`)
local Traverse = require(`../Util/Traverse`)
local SafeCall = require(`../Util/SafeCall`)

local Read = {}
local RootNode: Shared.Node?

local function ReadValue(value: any, valueType: string, propOrAtt: string?)
	local valueType = valueType or typeof(value)
	local call = Read[valueType]
	if not call then
		warn(`Type {valueType} of {propOrAtt and `{propOrAtt} ` or ``}{value} cannot be read`)
		return
	end

	return call(value)
end

function Read.string(input: string)
	return input
end

function Read.number(number: number)
	return number
end

function Read.boolean(bool: boolean)
	return bool
end

function Read.Color3(color: {number})
	return Color3.new(unpack(color))
end

function Read.Vector3(vector: {number})
	return Vector3.new(unpack(vector))
end

function Read.CFrame(cf: {number})
	return CFrame.new(unpack(cf))
end

function Read.UDim2(udim: {number})
	return UDim2.new(unpack(udim))
end

function Read.EnumItem(enumItem: string)
	local enumName, enumType, enumItem = unpack(string.split(enumItem, `.`))
	if not enumName or not enumType or not enumItem or enumName ~= `Enum` then
		warn(`Invalid EnumItem string {enumItem}`)
		return
	end

	return Enum[enumType][enumItem]
end


function Read.BrickColor(color: number)
	return BrickColor.new(color)
end

function Read.NumberSequence(seq: {[number]: {[string]: any}})
	local keypoints = {}
	for _, k in (seq) do
		table.insert(keypoints, NumberSequenceKeypoint.new(k.Time, k.Value, k.Envelope))
	end

	return NumberSequence.new(keypoints)
end

function Read.ColorSequence(seq: {[number]: {[string]: any}})
	local keypoints = {}
	for _, k in (seq) do
		table.insert(keypoints, ColorSequenceKeypoint.new(k.Time, Read.Color3(k.Color)))
	end

	return ColorSequence.new(keypoints)
end

function Read.NumberRange(range: {number})
	return NumberRange.new(unpack(range))
end

function Read.Rect(rect: {number})
	local min = Vector2.new(rect[1], rect[2])
	local max = Vector2.new(rect[3], rect[4])
	return Rect.new(min, max)
end

function Read.UDim(udim: {number})
	return UDim.new(unpack(udim))
end

function Read.Vector2(vector)
	return Vector2.new(unpack(vector))
end

function Read.Mesh(id: string)
	local cache = Traverse(`ReplicatedStorage.Assets.MeshCache`)
	local mesh: MeshPart? = cache:FindFirstChild(id)
	local skipProperties = {MeshId = true}
	if mesh then
		mesh = mesh:Clone()
	else
		local success, err = pcall(function()
			mesh = AssetService:CreateMeshPartAsync(Content.fromUri(id))
		end)
		
		if success then
			mesh.Name = id
			mesh.Parent = cache
			mesh = mesh:Clone()
		else
			warn(`Mesh fetch error {err}`)
			mesh = Instance.new(`Part`)
			skipProperties.TextureID = true
		end
	end
	
	return mesh, skipProperties
end

function Read.InstanceReference(node: Shared.Node, property: string)
	if node == nil or node.Properties[property] == nil then return end
	
	local path = string.split(node.Properties[property], `.`)
	local current = RootNode
	for _, move in (path) do
		current = current.Children[tonumber(move)]
	end
	
	repeat task.wait() until current.Object
	node.Object[property] = current.Object
	return
end

function Read.Instance(node: Shared.Node, direct: boolean)
	if not Instances[node.Class] then
		warn(`Unknown node with name {node.Properties.Name} of class {node.Class} will not be read`)
		return
	end
	
	local object: Instance?
	local skipProperties: {[string]: boolean} = {}
	if node.Class == `MeshPart` and node.Properties.MeshId and node.Properties.MeshId ~= `` then
		object, skipProperties = Read.Mesh(node.Properties.MeshId)
	else
		object = Instance.new(node.Class)
	end
	
	local availableProperties = Instances[object.ClassName]
	for _, propertyData in (availableProperties) do
		local property, propertyType, default = unpack(propertyData)
		if skipProperties[property] then continue end
		
		if propertyType == `InstanceReference` then
			task.spawn(Read.InstanceReference, node, property)
		else
			object[property] = node.Properties[property] == nil and default or ReadValue(node.Properties[property], propertyType, `property`)
		end
	end
	
	for attribute, value in (node.Attributes) do
		object:SetAttribute(attribute, ReadValue(value, nil, `attribute`))
	end
	
	for _, childNode in (node.Children) do
		local child = Read.Instance(childNode)
		if child == nil then continue end
		child.Parent = object
	end
	
	node.Object = object
	return object
end

function Read.Map(code: string)
	if string.sub(code, 1, 8) == `https://` then
		code = HttpService:GetAsync(code)
	end
	
	local compressedNodeTree = buffer.fromstring(code)
	local nodeTreeBuffer = EncodingService:DecompressBuffer(
		EncodingService:Base64Decode(compressedNodeTree), Enum.CompressionAlgorithm.Zstd
	)
	
	local clearCode = buffer.readstring(nodeTreeBuffer, 0, buffer.len(nodeTreeBuffer))
	local nodeTree = HttpService:JSONDecode(clearCode)
	
	RootNode = nodeTree
	local map = Read.Instance(nodeTree)
	return map
end

return Read]]></ProtectedString>
					<string name="ScriptGuid">{98976E70-DD40-4B6E-8843-97F1769755A2}</string>
					<BinaryString name="AttributesSerialize"></BinaryString>
					<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
					<bool name="DefinesCapabilities">false</bool>
					<string name="Name">Read</string>
					<int64 name="SourceAssetId">-1</int64>
					<BinaryString name="Tags"></BinaryString>
				</Properties>
			</Item>
			<Item class="ModuleScript" referent="RBXD0E8A69F1E1B4082ABFA9E02FAC7AB74">
				<Properties>
					<Content name="LinkedSource"><null></null></Content>
					<ProtectedString name="Source"><![CDATA[local Shared = {}

export type Node = {
	Class: string,
	Object: Instance?,
	Properties: {
		[string]: any
	},
	Attributes: {
		[string]: any
	},
	Children: {
		[number]: Node
	},
	Config: {
		[string]: any
	}?
}

return Shared]]></ProtectedString>
					<string name="ScriptGuid">{3B785BE4-ECD3-470B-97BB-42190DBF0596}</string>
					<BinaryString name="AttributesSerialize"></BinaryString>
					<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
					<bool name="DefinesCapabilities">false</bool>
					<string name="Name">Shared</string>
					<int64 name="SourceAssetId">-1</int64>
					<BinaryString name="Tags"></BinaryString>
				</Properties>
			</Item>
			<Item class="ModuleScript" referent="RBXA5B8327830B94712868E874E48A4E687">
				<Properties>
					<Content name="LinkedSource"><null></null></Content>
					<ProtectedString name="Source"><![CDATA[return {
	Part = {
		{"Name", "string", "Part"},
		{"Anchored", "boolean", false},
		{"CanCollide", "boolean", true},
		{"CanTouch", "boolean", true},
		{"CanQuery", "boolean", true},
		{"Massless", "boolean", false},
		{"Locked", "boolean", false},
		{"Transparency", "number", 0},
		{"Reflectance", "number", 0},
		{"CastShadow", "boolean", true},
		{"Material", "EnumItem", Enum.Material.Plastic},
		{"Color", "Color3", Color3.fromRGB(163, 162, 165)},
		{"Size", "Vector3", Vector3.new(4, 1, 2)},
		{"CFrame", "CFrame", CFrame.identity},
		{"Shape", "EnumItem", Enum.PartType.Block},
		{"TopSurface", "EnumItem", Enum.SurfaceType.Smooth},
		{"BottomSurface", "EnumItem", Enum.SurfaceType.Smooth},
		{"LeftSurface", "EnumItem", Enum.SurfaceType.Smooth},
		{"RightSurface", "EnumItem", Enum.SurfaceType.Smooth},
		{"FrontSurface", "EnumItem", Enum.SurfaceType.Smooth},
		{"BackSurface", "EnumItem", Enum.SurfaceType.Smooth},
	},

	MeshPart = {
		{"Name", "string", "MeshPart"},
		{"Anchored", "boolean", false},
		{"CanCollide", "boolean", true},
		{"CanTouch", "boolean", true},
		{"CanQuery", "boolean", true},
		{"Massless", "boolean", false},
		{"Transparency", "number", 0},
		{"Reflectance", "number", 0},
		{"CastShadow", "boolean", true},
		{"Material", "EnumItem", Enum.Material.Plastic},
		{"Color", "Color3", Color3.fromRGB(163, 162, 165)},
		{"Size", "Vector3", Vector3.new(1, 1, 1)},
		{"CFrame", "CFrame", CFrame.identity},
		{"MeshId", "string", ""},
		{"TextureID", "string", ""},
	},

	Model = {
		{"Name", "string", "Model"},
	},

	Folder = {
		{"Name", "string", "Folder"},
	},

	WedgePart = {
		{"Name", "string", "Wedge"},
		{"Anchored", "boolean", false},
		{"CanCollide", "boolean", true},
		{"CastShadow", "boolean", true},
		{"Transparency", "number", 0},
		{"Material", "EnumItem", Enum.Material.Plastic},
		{"Color", "Color3", Color3.fromRGB(163, 162, 165)},
		{"Size", "Vector3", Vector3.new(4, 1, 2)},
		{"CFrame", "CFrame", CFrame.identity},
	},

	CornerWedgePart = {
		{"Name", "string", "CornerWedge"},
		{"Anchored", "boolean", false},
		{"CanCollide", "boolean", true},
		{"CastShadow", "boolean", true},
		{"Transparency", "number", 0},
		{"Material", "EnumItem", Enum.Material.Plastic},
		{"Color", "Color3", Color3.fromRGB(163, 162, 165)},
		{"Size", "Vector3", Vector3.new(4, 1, 2)},
		{"CFrame", "CFrame", CFrame.identity},
	},

	TrussPart = {
		{"Name", "string", "Truss"},
		{"Anchored", "boolean", false},
		{"CanCollide", "boolean", true},
		{"CastShadow", "boolean", true},
		{"Transparency", "number", 0},
		{"Material", "EnumItem", Enum.Material.Metal},
		{"Color", "Color3", Color3.fromRGB(163, 162, 165)},
		{"Size", "Vector3", Vector3.new(2, 2, 2)},
		{"CFrame", "CFrame", CFrame.identity},
		{"Style", "EnumItem", Enum.Style.AlternatingSupports},
	},

	Decal = {
		{"Name", "string", "Decal"},
		{"Texture", "string", ""},
		{"Transparency", "number", 0},
		{"Color3", "Color3", Color3.new(1, 1, 1)},
		{"Face", "EnumItem", Enum.NormalId.Front},
	},

	Texture = {
		{"Name", "string", "Texture"},
		{"Texture", "string", ""},
		{"Transparency", "number", 0},
		{"Color3", "Color3", Color3.new(1, 1, 1)},
		{"Face", "EnumItem", Enum.NormalId.Front},
		{"StudsPerTileU", "number", 1},
		{"StudsPerTileV", "number", 1},
		{"OffsetStudsU", "number", 0},
		{"OffsetStudsV", "number", 0},
	},

	PointLight = {
		{"Name", "string", "PointLight"},
		{"Brightness", "number", 1},
		{"Range", "number", 8},
		{"Color", "Color3", Color3.new(1, 1, 1)},
		{"Enabled", "boolean", true},
		{"Shadows", "boolean", false},
	},

	SpotLight = {
		{"Name", "string", "SpotLight"},
		{"Brightness", "number", 1},
		{"Range", "number", 8},
		{"Angle", "number", 45},
		{"Face", "EnumItem", Enum.NormalId.Front},
		{"Color", "Color3", Color3.new(1, 1, 1)},
		{"Enabled", "boolean", true},
		{"Shadows", "boolean", false},
	},

	SurfaceLight = {
		{"Name", "string", "SurfaceLight"},
		{"Brightness", "number", 1},
		{"Range", "number", 8},
		{"Angle", "number", 90},
		{"Face", "EnumItem", Enum.NormalId.Front},
		{"Color", "Color3", Color3.new(1, 1, 1)},
		{"Enabled", "boolean", true},
		{"Shadows", "boolean", false},
	},

	Configuration = {
		{"Name", "string", "Configuration"}
	},

	Fire = {
		{"Color", "Color3", Color3.fromRGB(236, 139, 70)},
		{"Enabled", "boolean", true},
		{"Heat", "number", 9},
		{"Name", "string", "Fire"},
		{"SecondaryColor", "Color3", Color3.fromRGB(139, 80, 55)},
		{"Size", "number", 5},
		{"TimeScale", "number", 1}
	},

	ParticleEmitter = {
		{"Brightness", "number", 1},
		{"Color", "ColorSequence", ColorSequence.new(Color3.new(1, 1, 1))},
		{"LightEmission", "number", 0.5},
		{"LightInfluence", "number", 0},
		{"Orientation", "EnumItem", Enum.ParticleOrientation.FacingCamera},
		{"Size", "NumberSequence", NumberSequence.new(1)},
		{"Squash", "NumberSequence", NumberSequence.new(0)},
		{"Texture", "string", "rbxasset://textures/particles/sparkles_main.dds"},
		{"Transparency", "NumberSequence", NumberSequence.new(0)},
		{"ZOffset", "number", 0},
		{"EmissionDirection", "EnumItem", Enum.NormalId.Top},
		{"Enabled", "boolean", true},
		{"Lifetime", "NumberRange", NumberRange.new(5, 10)},
		{"Rate", "number", 20},
		{"Rotation", "NumberRange", NumberRange.new(0)},
		{"RotSpeed", "NumberRange", NumberRange.new(0)},
		{"Speed", "NumberRange", NumberRange.new(5)},
		{"SpreadAngle", "Vector2", Vector2.new(0, 0)},
		{"Shape", "EnumItem", Enum.ParticleEmitterShape.Box},
		{"ShapeInOut", "EnumItem", Enum.ParticleEmitterShapeInOut.Outward},
		{"ShapeStyle", "EnumItem", Enum.ParticleEmitterShapeStyle.Volume},
		{"Acceleration", "Vector3", Vector3.new(0, 0, 0)},
		{"Drag", "number", 0},
		{"LockedToPart", "boolean", false},
		{"TimeScale", "number", 1},
		{"VelocityInheritance", "number", 0},
		{"WindAffectsDrag", "boolean", 0},
	},

	SpecialMesh = {
		{"MeshId", "string", ""},
		{"MeshType", "EnumItem", Enum.MeshType.Head},
		{"Name", "string", "SpecialMesh"},
		{"Offset", "Vector3", Vector3.new(0, 0, 0)},
		{"Scale", "Vector3", Vector3.new(1, 1, 1)},
		{"VertexColor", "Vector3", Vector3.new(1, 1, 1)}
	},

	Sky = {
		{"CelestialBodiesShown", "boolean", true},
		{"MoonAngularSize", "number", 11},
		{"MoonTextureId", "string", "rbxasset://sky/moon.jpg"},
		{"SkyboxBk", "string", "rbxasset://textures/sky/sky512_bk.tex"},
		{"SkyboxDn", "string", "rbxasset://textures/sky/sky512_dn.tex"},
		{"SkyboxFt", "string", "rbxasset://textures/sky/sky512_ft.tex"},
		{"SkyboxLf", "string", "rbxasset://textures/sky/sky512_lf.tex"},
		{"SkyboxOrientation", "Vector3", Vector3.new(0, 0, 0)},
		{"SkyboxRt", "string", "rbxasset://textures/sky/sky512_rt.tex"},
		{"SkyboxUp", "string", "rbxasset://textures/sky/sky512_up.tex"},
		{"StarCount", "number", 3000},
		{"SunAngularSize", "number", 21},
		{"SunTextureId", "string", "rbxasset://sky/sun.jpg"},
	},

	Color3Value = {
		{"Name", "string", "Color3Value"},
		{"Value", "Color3", Color3.new()},
	},

	NumberValue = {
		{"Name", "string", "NumberValue"},
		{"Value", "number", 0},
	},
	
	BlurEffect = {
		{"Name", "string", "Blur"},
		{"Size", "number", 24}
	},

	BloomEffect = {
		{"Name", "string", "Bloom"},
		{"Enabled", "boolean", true},
		{"Intensity", "number", 1},
		{"Size", "number", 24},
		{"Threshold", "number", 2}
	},

	Atmosphere = {
		{"Name", "string", "Atmosphere"},
		{"Density", "number", 0.395},
		{"Offset", "number", 0},
		{"Color", "Color3", Color3.fromRGB(199, 170, 107)},
		{"Decay", "Color3", Color3.fromRGB(92, 60, 13)},
		{"Glare", "number", 0},
		{"Haze", "number", 0}
	},

	DepthOfFieldEffect = {
		{"Name", "string", "DepthOfField"},
		{"Enabled", "boolean", true},
		{"FarIntensity", "number", 0.75},
		{"FocusDistance", "number", 0.05},
		{"InFocusRadius", "number", 10},
		{"NearIntensity", "number", 0.75}
	},

	SunRaysEffect = {
		{"Name", "string", "SunRaysEffect"},
		{"Enabled", "boolean", true},
		{"Intensity", "number", 0.25},
		{"Spread", "number", 1}
	},

	ColorCorrectionEffect = {
		{"Name", "string", "ColorCorrection"},
		{"Brightness", "number", 0},
		{"Contrast", "number", 0},
		{"Enabled", "boolean", false},
		{"Saturation", "number", 0},
		{"TintColor", "Color3", Color3.new(1, 1, 1)}
	},
	
	ColorGradingEffect = {
		{"Name", "string", "ColorGrading"},
		{"TonemapperPreset", "EnumItem", Enum.TonemapperPreset.Default}
	},

	Attachment = {
		{"Visible", "boolean", false},
		{"Name", "string", "Attachment"},
		{"CFrame", "CFrame", CFrame.new()},
	},

	BoolValue = {
		{"Name", "string", "Value"},
		{"Value", "boolean", false}
	},

	Vector3Value = {
		{"Name", "string", "Value"},
		{"Value", "Vector3", Vector3.new()}
	},

	SurfaceGui = {
		{"ZOffset", "number", 0},
		{"Active", "boolean", true},
		{"Adornee", "InstanceReference", nil},
		{"Enabled", "boolean", true},
		{"Face", "EnumItem", Enum.NormalId.Front},
		{"LightInfluence", "number", 1},
		{"MaxDistance", "number", 1000},
		{"Name", "string", "SurfaceGui"},
		{"ResetOnSpawn", "boolean", true},
		{"ToolPunchThroughDistance", "boolean", false},
		{"ZIndexBehavior", "EnumItem", Enum.ZIndexBehavior.Sibling},
		{"ClipsDescendants", "boolean", true},
		{"SelectionGroup", "boolean", true},
		{"PixelsPerStud", "number", 50},
		{"SizingMode", "EnumItem", Enum.SurfaceGuiSizingMode.PixelsPerStud}
	},

	Sound = {
		{"Name", "string", "Sound"},
		{"PlayOnRemove", "boolean", false},
		{"SoundId", "string", ""},
		{"RollOffMaxDistance", "number", 10000},
		{"RollOffMinDistance", "number", 10},
		{"RollOffMode", "EnumItem", Enum.RollOffMode.Linear},
		{"Looped", "boolean", false},
		{"PlaybackRegionsEnabled", "boolean", false},
		{"PlaybackSpeed", "number", 1},
		{"Playing", "boolean", false},
		{"TimePosition", "number", 0},
		{"Volume", "number", 0.5}
	},

	Beam = {
		{"Color", "ColorSequence", ColorSequence.new(Color3.new(1, 1, 1))},
		{"Enabled", "boolean", true},
		{"LightEmission", "number", 0},
		{"LightInfluence", "number", 1},
		{"Texture", "string", ""},
		{"TextureLength", "number", 1},
		{"TextureMode", "EnumItem", Enum.TextureMode.Stretch},
		{"TextureSpeed", "number", 1},
		{"Transparency", "NumberSequence", NumberSequence.new(0.5)},
		{"ZOffset", "number", 0},
		{"Attachment0", "InstanceReference", nil},
		{"Attachment1", "InstanceReference", nil},
		{"CurveSize0", "number", 0},
		{"CurveSize1", "number", 0},
		{"FaceCamera", "boolean", false},
		{"Segments", "number", 10},
		{"Width0", "number", 1},
		{"Width1", "number", 1},
		{"Name", "string", "Beam"}
	},

	Weld = {
		{"C0", "CFrame", CFrame.new()},
		{"C1", "CFrame", CFrame.new()},
		{"Part0", "InstanceReference", nil},
		{"Part1", "InstanceReference", nil},
		{"Enabled", "boolean", true},
		{"Name", "string", "Weld"}
	},

	WeldConstraint = {
		{"Part0", "InstanceReference", nil},
		{"Part1", "InstanceReference", nil},
		{"Enabled", "boolean", true},
		{"Name", "string", "WeldConstraint"}
	},

	Humanoid = {
		{"AutoJumpEnabled", "boolean", true},
		{"AutoRotate", "boolean", true},
		{"BreakJointsOnDeath", "boolean", true},
		{"DisplayDistanceType", "EnumItem", Enum.HumanoidDisplayDistanceType.Viewer},
		{"Health", "number", 100},
		{"HealthDisplayDistance", "number", 100},
		{"HealthDisplayType", "EnumItem", Enum.HumanoidHealthDisplayType.DisplayWhenDamaged},
		{"HipHeight", "number", 0},
		{"JumpHeight", "number", 7.2},
		{"JumpPower", "number", 50},
		{"MaxHealth", "number", 100},
		{"NameDisplayDistance", "number", 100},
		{"NameOcclusion", "EnumItem", Enum.NameOcclusion.OccludeAll},
		{"PlatformStand", "boolean", false},
		{"RequiresNeck", "boolean", true},
		{"RigType", "EnumItem", Enum.HumanoidRigType.R6},
		{"Sit", "boolean", false},
		{"UseJumpPower", "boolean", true},
		{"WalkSpeed", "number", 16},
		{"Name", "string", "Humanoid"}
	},

	Motor6D = {
		{"C0", "CFrame", CFrame.new()},
		{"C1", "CFrame", CFrame.new()},
		{"Part0", "InstanceReference", nil},
		{"Part1", "InstanceReference", nil},
		{"Enabled", "boolean", true},
		{"Transform", "CFrame", CFrame.new()},
		{"DesiredAngle", "number", 0},
		{"CurrentAngle", "number", 0},
		{"MaxVelocity", "number", 0.1},
		{"Name", "string", "Motor6D"}
	},

	Snap = {
		{"C0", "CFrame", CFrame.new()},
		{"C1", "CFrame", CFrame.new()},
		{"Part0", "InstanceReference", nil},
		{"Part1", "InstanceReference", nil},
		{"Enabled", "boolean", true},
		{"Name", "string", "Snap"}
	},

	Animator = {
		{"PreferLodEnabled", "boolean", true},
		{"Name", "string", "Animator"}
	},

	Shirt = {
		{"Color3", "Color3", Color3.new(1, 1, 1)},
		{"ShirtTemplate", "string", ""},
		{"Name", "string", "Shirt"},
	},

	Pants = {
		{"Color3", "Color3", Color3.new(1, 1, 1)},
		{"PantsTemplate", "string", ""},
		{"Name", "string", "Pants"}
	},

	HingeConstraint = {
		{"Color", "BrickColor", BrickColor.new()},
		{"Radius", "number", 0.15},
		{"Visible", "boolean", false},
		{"Enabled", "boolean", true},
		{"Attachment0", "InstanceReference", nil},
		{"Attachment1", "InstanceReference", nil},
		{"ActuatorType", "EnumItem", Enum.ActuatorType.None},
		{"LimitsEnabled", "boolean", false},
		{"Name", "string", "HingeConstraint"}
	},

	Lighting = {
		{"Ambient", "Color3", Color3.fromRGB(138, 138, 138)},
		{"Brightness", "number", 2},
		{"ColorShift_Bottom", "Color3", Color3.new()},
		{"ColorShift_Top", "Color3", Color3.new()},
		{"EnvironmentDiffuseScale", "number", 0}, 
		{"EnvironmentSpecularScale", "number", 0},
		{"GlobalShadows", "boolean", true},
		{"OutdoorAmbient", "Color3", Color3.fromRGB(128, 128, 128)},
		{"ClockTime", "number", 14},
		{"GeographicLatitude", "number", 56.45},
		{"ExposureCompensation", "number", 0},
		{"FogColor", "Color3", Color3.fromRGB(192, 192, 192)},
		{"FogEnd", "number", 100000},
		{"FogStart", "number", 0}
	}
}]]></ProtectedString>
					<string name="ScriptGuid">{883FDFCA-C10E-4D41-AD38-80E04EEB7D6D}</string>
					<BinaryString name="AttributesSerialize"></BinaryString>
					<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
					<bool name="DefinesCapabilities">false</bool>
					<string name="Name">Instances</string>
					<int64 name="SourceAssetId">-1</int64>
					<BinaryString name="Tags"></BinaryString>
				</Properties>
			</Item>
			<Item class="ModuleScript" referent="RBX891CDE25D1A24658A5893C0291F263ED">
				<Properties>
					<Content name="LinkedSource"><null></null></Content>
					<ProtectedString name="Source"><![CDATA[local CoreGui = game:GetService("CoreGui")
local ScriptEditorService = game:GetService("ScriptEditorService")
local Selection = game:GetService("Selection")

local Write = require(`./Write`)
local Read = require(`./Read`)
local Current = require(`../Util/Current`)
local FeatureCheck = require(`../Util/FeatureCheck`)

local Create = require(`../Util/UI/Create`)
local Frame = require(`../Util/UI/Frame`)
local Label = require(`../Util/UI/Label`)
local Button = require(`../Util/UI/Button`)
local TextBox = require(`../Util/UI/TextBox`)

local Serializer = {}
Serializer.Priority = 1
Serializer.Name = `Serializer` :: string
Serializer.Description = `Serializes your map into a string, allowing you to easily test it in-game` :: string
Serializer.Icon = 127938367765919 :: number

function Serializer:Activate()
	local SerializeButton
	local ReadbackInput
	local PreviewButton
	SerializeButton = Button {
		Text = `Serialize`,

		Size = UDim2.fromOffset(200, 30),
		Position = UDim2.fromOffset(50, 50),

		Activated = function()
			local previous = workspace:FindFirstChild(`Scarlet Map Code`)
			if previous and previous:IsA(`Script`) and previous.Disabled then
				previous:Destroy()
			end
			
			local current = Current:Get()
			if not current then return end
			
			local code = Write.Map(current)
			local copy = Instance.new(`Script`)
			copy.Disabled = true
			ScriptEditorService:UpdateSourceAsync(copy, function()
				return code
			end)
			copy.Name = `Scarlet Map Code`
			copy.Parent = workspace
			Selection:Set({copy})
			ScriptEditorService:OpenScriptDocumentAsync(copy)
		end,
	}
	ReadbackInput = TextBox {
		PlaceholderText = `Readback`,

		Size = UDim2.fromOffset(200, 30),
		Position = UDim2.fromOffset(270, 50),

		FocusLost = function(enter: boolean)
			if not enter then return end

			local code = ReadbackInput.Text
			local map = Read.Map(code)
			if not map then return end
			
			map.Parent = workspace
		end,
	}
	PreviewButton = Button {
		Text = `Preview`,
		
		Size = UDim2.fromOffset(200, 30),
		Position = UDim2.new(0, 50, 1, -80),
		
		Activated = function()
			local current = Current:Get()
			if not current then return end
			
			local code = Write.Map(current)
			local map = Read.Map(code)
			if not map then return end
			
			map.Parent = workspace
		end,
	}
	
	self.UI = Create "ScreenGui" {
		IgnoreGuiInset = true,
		
		Parent = CoreGui,
		Children = {
			SerializeButton,
			ReadbackInput,
			FeatureCheck(`Debug`) and PreviewButton or nil,
		}
	}
end

function Serializer:Deactivate()
	if self.UI then
		self.UI:Destroy()
		self.UI = nil
	end
end

return Serializer]]></ProtectedString>
					<string name="ScriptGuid">{639C83CE-81C5-49BB-971B-E4456FDE4C3C}</string>
					<BinaryString name="AttributesSerialize"></BinaryString>
					<SecurityCapabilities name="Capabilities">0</SecurityCapabilities>
					<bool name="DefinesCapabilities">false</bool>
					<string name="Name">Init</string>
					<int64 name="SourceAssetId">-1</int64>
					<BinaryString name="Tags"></BinaryString>
				</Properties>
			</Item>
		</Item>
	</Item>
</roblox>